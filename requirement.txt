# Bỏ version pin cho pandas, pyarrow vì Airflow 2.8.2 đã pin sẵn các phiên bản tương thích
# Chỉ pin pyspark ở version cụ thể vì Airflow không quản lý nó
requests>=2.31.0
fastparquet>=2024.2.0
pyspark==3.5.0
